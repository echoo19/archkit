---
description: "Rescan the repository and update all .archkit/ artifacts to reflect structural changes"
---

Invoke the `archkit:refresh` skill to rescan the repo and update all `.archkit/` artifacts.

Use this after major refactors, adding new directories, or when routing results feel stale. Preserves existing zone configuration while updating file inventory and zone confidence scores.
