---
description: "Initialize ArchKit for this repository — scan structure, infer zones, and generate .archkit/ agent navigation artifacts"
---

Invoke the `archkit:init` skill to scan this repository and generate the `.archkit/` directory with agent navigation artifacts. This only needs to run once per repository (or after a major restructure).
